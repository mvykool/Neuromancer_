import Navbar from "../components/navbar/Navbar";

const HomeView = () => {
  return (
    <main className="bg-black h-screen w-full relative flex flex-col items-center px-[10%]">
      <Navbar />
    </main>
  );
};

export default HomeView;
